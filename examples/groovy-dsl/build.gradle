plugins {
    id "java"
    id "net.spark.plugins"
}

repositories {
    mavenCentral()
}

configurations {
    testIntImplementation
}

dependencies {
    testImplementation("org.junit.jupiter:junit-jupiter:5.9.0-M1")
    testIntImplementation("org.junit.jupiter:junit-jupiter:5.9.0-M1")
}

spark {
    test {
        it.module("testInt") { it.useJunitPlatform() }
    }
}
